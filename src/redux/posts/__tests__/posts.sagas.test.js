import { call } from "redux-saga/effects";
import { fetchPostsStart, fetchPostsSuccess } from "../posts.actions";

describe("getPosts", () => {
  it("successfully triggers clear messages action after getting albums", () => {});
});
